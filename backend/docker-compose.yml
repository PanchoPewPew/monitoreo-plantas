# docker-compose solo para el backend (standalone).
# Para levantar backend + frontend juntos usa el docker-compose.yml de la raíz del proyecto.
services:
  api:
    build: .
    container_name: backend-monitoreo
    ports:
      - "8888:8000"
    env_file:
      - .env
    restart: unless-stopped
